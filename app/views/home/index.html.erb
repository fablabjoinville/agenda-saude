<div class="row text-center">
  <div class="col">
    <h2>Sistema de agendamento de vacinas</h2>
  </div>
</div>
<div class="row text-center">
  <div class="col">
    <%= form_for(:patient, url: patient_base_login_path) do |f| %>
      <div class="field">
        <%= f.label :cpf, "CPF (somente números)" %><br />
        <%= f.text_field :cpf, autofocus: true, autocomplete: "cpf", minlength: 11, maxlength: 11 %>
      </div>

      <div class="actions">
        <%= f.submit "Entrar", class: "btn btn-default" %>
      </div>
    <% end %>
  </div>
</div>
