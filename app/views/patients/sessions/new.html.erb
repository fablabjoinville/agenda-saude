<h2>Sign in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :cpf, "CPF (somente números)" %><br />
    <%= f.text_field :cpf, autofocus: true, autocomplete: "cpf", minlength: 11, maxlength: 11 %>
  </div>

  <div class="actions">
    <%= f.submit "Entrar" %>
  </div>
<% end %>

<a href="<%= new_user_session_path %>">login operador</a><br />
